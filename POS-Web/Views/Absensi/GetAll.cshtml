@model IEnumerable<POS_Web.DataContext.AbsensiEntity>

@{
    ViewBag.Title = "Absensi";
    Layout = "_Layout";
}

<div class="card-header">
    <h3>Absensi</h3>
</div>
<div class="card-body">
    <div class="table-responsive">
        <a class="btn btn-sm btn-primary" asp-area="" asp-controller="Absensi" asp-action="Add">New Absensi</a>
        <table class="table table-hover mb-0">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>EmployeeId</th>
                    <th>AbsenStartDate</th>
                    <th>AbsenEndDate</th>
                    <th>Location</th>
                    <th>Description</th>
                    <th class="text-center">Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.EmployeeId</td>
                        <td>@item.AbsenStartDate</td>
                        <td>@item.AbsenEndDate</td>
                        <td>@item.Location</td>
                        <td>@item.Description</td>
                        <td class="text-center">
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Edit</a>
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-success">View</a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
